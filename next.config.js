/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'digitalmarketplace-production.up.railway.app',
        ],
    },
}

module.exports = nextConfig
