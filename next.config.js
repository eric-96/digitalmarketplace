/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'digitalmarket-production.up.railway.app',
        ],
    },
}

module.exports = nextConfig
